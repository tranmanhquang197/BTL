import {Country} from './country.model';

export class Store {
  code: string | undefined | null;
  name: string | undefined | null;
  dc: boolean | undefined | null;

  // constructor(code: string, name: string) {
  //   this.code = code;
  //   this.name = name;
  // }
}
